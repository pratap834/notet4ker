# Core NLP libraries
# Pin versions that have pre-built wheels to avoid compilation
spacy==3.7.2
blis==0.7.11  # Pin to specific version with pre-built wheels
cymem==2.0.8
murmurhash==1.0.10
preshed==3.0.9
thinc==8.2.2  # Compatible with spacy 3.7.2
scispacy>=0.5.3
transformers>=4.35.0
torch>=2.1.0,<2.5.0  # Use stable version with better wheel support
sentence-transformers>=2.2.0
accelerate>=0.20.0
safetensors>=0.4.0

# LLM Models (installed automatically via transformers)
# - ClinicalBERT: emilyalsentzer/Bio_ClinicalBERT
# - FLAN-T5: google/flan-t5-base, google/flan-t5-large

# Medical NLP models (install separately via commands in README)
# en_core_sci_sm (scispacy)
# en_ner_bc5cdr_md (scispacy)

# Data processing
pandas>=2.0.0
numpy>=1.24.0
scikit-learn>=1.3.0
dateparser>=1.1.0

# Keyword extraction
yake>=0.4.8
rake-nltk>=1.0.6

# API and serving
flask>=3.0.0
gunicorn>=21.2.0
python-multipart>=0.0.6

# Testing
pytest>=7.4.0
pytest-cov>=4.1.0
pytest-asyncio>=0.21.0

# Utilities
python-dotenv>=1.0.0
tqdm>=4.66.0
